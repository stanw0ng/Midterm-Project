<%- include('partials/_header', {title: "Read Stories - Open Book"}); %>

<div class = 'container'>
  <section class = 'all-stories'>
    <h2>All Stories</h2>
    <ul>
      <% allStories.forEach(story => { %>
        <li><a href="/stories/<%= story.id %>"><%= story.story_title %></a></li>
      <% }); %>
    </ul>
  </section>
  <section class = 'my-stories'>
    <h2>My Stories</h2>
    <ul>
      <% myStories.forEach(story => { %>
        <li><a href="/stories/<%= story.id %>"><%= story.story_title %></a></li>
      <% }); %>
    </ul>
  </section>
</div>

<%- include('partials/_footer'); %>
